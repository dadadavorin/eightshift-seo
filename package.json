{
	"name": "eightshift-seo",
	"version": "1.0.0",
	"description": "Eightshift SEO plugin admin and editor assets",
	"private": true,
	"scripts": {
		"build": "wp-scripts build --config webpack.config.js",
		"build:production": "cross-env NODE_ENV=production wp-scripts build --config webpack.config.js",
		"start": "wp-scripts start --config webpack.config.js"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.0.0",
		"cross-env": "^7.0.3"
	},
	"dependencies": {
		"@eightshift/ui-components": "^2.0.0"
	}
}
